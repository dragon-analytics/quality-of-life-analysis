docker run -it --rm -p 8888:8888 jupyter/datascience-notebook
New Terminal: pip install pymongo
New notebook: import pymongo
cd MongoConda
docker-compuse build
docker ps
contid=$(docker ps | grep mongoconda_web | awk '{print $1}')
